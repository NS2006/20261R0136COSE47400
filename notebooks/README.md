# Notebooks

Use this folder for exploratory work, visual inspection, and early experiments.

Possible notebooks:

- Baseline SuperAD analysis
- DINOv2 feature extraction checks
- SVD / orthogonal projection verification
- Category-wise `k_bg` and `k_normal` tuning

Move reusable implementation into `src/` rather than leaving final code only in notebooks.

# Source Code

This folder will contain implementation files for the proposed anomaly detection model.

## Planned Modules

```text
src/
├── feature_extraction/   # DINOv2 feature extraction
├── memory_bank/          # Normal and background bank construction
├── projection/           # SVD and orthogonal projection logic
├── inference/            # Anomaly score calculation and image mapping
└── utils/                # Shared utilities
```

Add Python source files only after the architecture and experiment pipeline are finalized.
